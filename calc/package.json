{
  "name": "@pkmn/calc",
  "version": "0.0.1",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "description": "Adapater for using Pokémon Showdown data with @smogon/damage-calc",
  "repository": "github:pkmn/ps",
  "author": "Kirk Scheibelhut <pre@pkmn.cc>",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@pkmn/types": "^0.1.0",
    "@smogon/calc": "^0.3.0"
  },
  "devDependencies": {
    "@pkmn/dex": "^0.0.1"
  },
  "scripts": {
    "lint": "eslint --cache src/**/*.ts",
    "fix": "eslint --fix src/**/*.ts",
    "compile": "tsc -p .",
    "bundle": "node bundle",
    "build": "npm run compile && npm run bundle",
    "test": "jest",
    "prepare": "npm run build",
    "pretest": "npm run build",
    "posttest": "npm run lint"
  }
}
