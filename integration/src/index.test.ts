// HAVE STANDALONE CODE WHICH runs this -> can call binary to run infinitely

describe('Integration', () => {
  it('test', () => {
  // 1. run exhaustive test (from same seed) with vendor and pkg in lockstep
  // 2. compare output
  // 3. compare protocol
  });
});
