{
  "name": "@pkmn/protocol",
  "version": "0.0.1",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "description": "Parsing logic for Pokémon Showdown's PROTOCOL and SIM-PROTOCOL",
  "repository": "github:pkmn/ps",
  "author": "Kirk Scheibelhut <pre@pkmn.cc>",
  "license": "MIT",
  "dependencies": {
    "@pkmn/types": "file:../types"
  },
  "devDependencies": {
    "@pkmn/eslint-config": "^0.2.2",
    "@types/jest": "^25.1.4",
    "@typescript-eslint/eslint-plugin": "^2.24.0",
    "eslint-plugin-import": "^2.20.1",
    "eslint-plugin-jest": "^23.8.2",
    "eslint": "^6.8.0",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.1",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "lint": "eslint --cache index.ts index.test.ts",
    "fix": "eslint --fix index.ts index.test.ts",
    "compile": "tsc -p .",
    "test": "jest",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
